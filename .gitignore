node_modules/
*.log
.DS_Store
.env
.env.*
# local user config / state should never be committed
config.json
*.local

# built menu bar app (prebuilt+signed only at release time)
menubar/dist/

# local promo / scratch video assets (never published; not in npm "files")
assets/x-promo*.mp4
assets/promo-build/
